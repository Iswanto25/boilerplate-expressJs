# Minimal Environment Configuration (Without Third-Party Services)
# This demonstrates that the app can run without Redis, S3/MinIO, and SMTP

# Database (REQUIRED)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Common (REQUIRED)
NODE_ENV=development
PORT=3004
HOST=0.0.0.0
BASE_URL=http://localhost:3004
DOMAIN=localhost
version=1.0.0

# Security (REQUIRED)
DATA_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef

# JWT (REQUIRED)
JWT_SECRET=your-jwt-secret-change-this-in-production
JWT_REFRESH_SECRET=your-refresh-secret-change-this-in-production
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Note: Redis, S3/MinIO, and SMTP are NOT configured
# The application will run normally with warnings in the logs
# - Rate limiting will be skipped
# - Token storage will be skipped
# - File upload routes will return 503 errors
# - Email sending will be skipped
